#include <stdio.h>
#include <stdlib.h>

int main ( ){
    // FILE *fp = fopen("myfile.txt","r");
    // char str[80];
    

    // if( fgets(str,10,fp) != NULL) {

    //     printf("%s",str);

    // }    
    // fclose(fp);

    FILE *fp = fopen("creat.txt","w");

    return 0;  
}